# Sample App 작업 지침

## 기본 작업 방식

- 작업을 시작하기 전에 기존 프로젝트 구조와 관련 코드를 먼저 분석한다.
- 요구사항이 불명확하고 결과가 크게 달라질 수 있을 때만 질문한다.
- 기존 기능, 디자인, API 동작을 임의로 삭제하거나 변경하지 않는다.
- 최소 범위로 수정하고 관련 없는 파일은 건드리지 않는다.

## Git 작업

- main 브랜치에 직접 커밋하거나 push하지 않는다.
- 작업마다 feature 브랜치를 생성한다.
- 브랜치 이름은 `feature/<기능명>` 형식을 사용한다.
- 작업 완료 후 변경 내역을 요약하고 Pull Request를 생성한다.
- 사용자의 확인 없이 main에 병합하지 않는다.
- 충돌 발생 시 임의로 선택하여 해결하지 말고 충돌 원인과 선택지를 설명한다.

## 구현 및 검증

- 기존 코드 스타일과 컴포넌트 구조를 따른다.
- Supabase 스키마와 기존 데이터 접근 방식을 먼저 확인한다.
- 하드코딩된 URL, API Key, Service Role Key를 코드에 추가하지 않는다.
- 환경변수 이름은 기존 프로젝트 규칙을 따른다.
- 수정 후 가능한 범위에서 lint, typecheck, test, production build를 실행한다.
- 검증 실패를 숨기지 말고 오류와 미검증 항목을 보고한다.

## 완료 조건

작업 완료 시 다음 내용을 보고한다.

1. 변경한 기능
2. 변경한 주요 파일
3. 실행한 검증과 결과
4. 환경변수 또는 DB 변경 필요 여부
5. 남아 있는 위험이나 확인 사항
6. Pull Request 링크

## 배포

- Vercel Preview에서 확인할 수 있도록 PR 기반으로 작업한다.
- 사용자의 승인 없이 프로덕션 배포 설정을 변경하지 않는다.